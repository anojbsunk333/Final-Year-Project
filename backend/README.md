# Backend

Simple Express backend that serves mock data from `mockData.js`.

Quick start:

```bash
cd backend
npm install
npm run dev   # or npm start
```

API:

- `GET /` - health check
- `GET /api/users` - returns mock users
