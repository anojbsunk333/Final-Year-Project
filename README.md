# Project

Scaffolded frontend and backend structure for the TRI·NETRA project.

- Backend: see `backend/`
- Frontend: see `frontend/`

Run servers:

Backend:

```bash
cd backend
npm install
npm run dev
```

Frontend:

```bash
cd frontend
npm install
npm run dev
```
