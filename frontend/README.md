# Frontend

This is a Vite + React frontend. It expects the backend at `http://localhost:4000` by default.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

Environment:

- `VITE_API_BASE` — set to backend base URL (e.g. `http://localhost:4000`)
